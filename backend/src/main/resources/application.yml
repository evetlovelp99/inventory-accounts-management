spring:
  application:
    name: beewax-backend
  profiles:
    active: dev
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: ${JWT_SECRET:dev-only-change-me-beewax-jwt-secret-key-minimum-256-bits!!}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

server:
  port: 8080
